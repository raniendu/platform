Table threads {
  interface             text    [not null]
  external_thread_id    text    [not null]
  agent_name            text    [not null]
  message_history_json  blob    [note: 'ADK session_id/user_id JSON; NULL after /reset.']
  created_at            text    [not null]
  updated_at            text    [not null]

  indexes {
    (interface, external_thread_id) [pk]
  }
}

Table telegram_updates {
  update_id   integer [pk]
  created_at  text    [not null]
}

// ADK DatabaseSessionService owns its own tables in .vikram/adk_sessions.sqlite3.
