name = "Leo"
description = "Group assistant agent for Telegram chats."
system_prompt = "system_prompt.md"

# Local resources — relative to spec/leo/
context_files = []
skills = []

# Shared resources — relative to spec/shared/
shared_context_files = ["context/production.md"]
shared_skills = []

# Tools — names resolved from raman.tools.TOOL_REGISTRY
tools = ["web_search"]

# Model knobs — passed verbatim to pydantic_ai ModelSettings.
[model_settings]
temperature = 0.3
